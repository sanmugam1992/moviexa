Moviexa Full App

1) backend/.env -> add TMDB_API_KEY
2) cd backend && npm install
3) node server.js
4) Copy frontend/index.html to /var/www/moviexa
5) Configure nginx with nginx/moviexa.conf
6) Open http://SERVER_IP
